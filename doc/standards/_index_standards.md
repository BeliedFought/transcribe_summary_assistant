# Индекс пакета стандартов. Версия 4.11.0

Точка входа в пакет стандартов. Этот файл тегают AI-агенту, чтобы он определил, какие документы пакета релевантны текущей задаче. Пакет состоит из основного стандарта и дополнений: каждое дополнение применяется поверх `project_standards.md` для своего класса проектов. Агент выбирает только те документы, которые относятся к задаче, и не загружает пакет целиком без необходимости.

Пакет имеет единую версию (см. раздел «Политика версионирования»). Версия во всех документах пакета, в этом индексе, а также в `AGENTS.md` и `README.md` проекта (и в `CLAUDE.md`, если файл есть) - одна и та же.

Порядок принятия решений агентом при отсутствии или неоднозначности нормы - `project_standards.md`, раздел 07.06 (иерархия источников: указания диалога -> индексы -> оверрайды -> прецедент репозитория -> интернет -> практики -> собственное решение; проверка фактов вместо выдумывания).

---

## Обязательность применения

Тегание этого файла (`@doc/standards/_index_standards.md`) означает **обязательное применение** стандартов пакета, а не просто ознакомление с ними. Агент:

1. Определяет релевантные документы по карте маршрутизации
2. Загружает указанные документы и применяет их требования к текущей задаче
3. Соблюдает все правила и ограничения, описанные в стандартах
4. Проверяет соответствие результата стандартам перед завершением работы
5. Передает требования стандартов субагентам; субагенты обязаны применять стандарты в том же объеме, что и основной агент

Стандарты применяются всегда при работе с проектом, независимо от того, был ли явно тегнут этот файл. Тегание лишь указывает агенту на необходимость явно загрузить и применить пакет.

Субагенты (запущенные через task) применяют стандарты пакета наравне с основным агентом.

---

## Реестр документов

| Документ | Назначение | Применимость | Когда применять |
|----------|-----------|--------------|-----------------|
| `project_standards.md` | Основной стандарт: структура проекта, конфигурация, код, вывод и интерфейс, безопасность, git, рабочий процесс | Обязательно для всех проектов. Проекты могут быть разными по типу и стеку - стандарт универсален; разделы, привязанные к Python, применяются в Python-проектах, отклонения остальных - через оверрайды (01.01.02) | Инициализация проекта; любая работа с кодом, структурой, выводом, git |
| `console_ui_standards.md` | Контракт консольного UI (кадр, клавиши, роли, таблицы/списки/сводки, два канала, batch rich-progress) и обязательный `ui_spec.html` | Обязательно при menu/rich UI; артефакт `ui_spec.html` - для всех репозиториев | Интерактивное меню, rich-консоль, batch CLI, макеты консоли/лога, реверс UI |
| `deploy_standards.md` | Общее OS-agnostic ядро деплоя Python-проекта как системного инструмента (в т.ч. миграция config/.env при update) | Опционально - для проектов, устанавливаемых как CLI-инструмент | Подготовка проекта к установке; правила pyproject.toml, package-data, PROJECT_ROOT, политика update |
| `deploy_lin_standards.md` | OS-overlay: деплой на Linux; update.py - миграция config.ini/.env по example | Опционально - при установке на Linux | Установка на Linux: прямая установка через uv, пути XDG, update/миграция |
| `deploy_win_standards.md` | OS-overlay: деплой на Windows; update.py - та же миграция относительно шаблонов архива | Опционально - при установке на Windows | Установка на Windows: сборка wheel и ZIP на Linux, установка на Windows, update/миграция |
| `deploy_docker_standards.md` | Развертывание сервисов в Docker; публикация портов и привязка к интерфейсу хоста, reverse proxy для доступа из интернета (ufw на опубликованные порты не действует); машинные оверрайды документации (`override/<hostname>/`) | Опционально - для Docker-инфраструктуры | Настройка Docker-сервисов, compose, тома, сеть, публикация портов, бэкап; кастомизации под хост |
| `web_standards.md` | Разработка и развертывание веб-приложений: автономная подсистема `web/` (FastAPI + uvicorn), фронты nginx (в т.ч. несколько фронтов с одним бэкендом), модели деплоя хост/systemd и Docker, TLS, обновление и откат | Опционально - для веб-проектов | Разработка веб-приложения; структура репо веб-проекта; локальный запуск; деплой веб-приложения (systemd + nginx, Docker), конфигурация nginx и фронтов, TLS, файрвол, обновление |
| `skill_plain_standards.md` | Правила создания и оформления plain-навыков (плоский формат) AI-агента (в т.ч. английские имена файлов без транслита, лимит объема до 800 строк, таксономия типов и обязательное поле `type` в frontmatter, запрет упоминаний навыков чужих репозиториев и кириллических плейсхолдеров) | Опционально - при наличии навыков | Создание, обновление или редактирование plain-навыков (плоские файлы в `doc/skills/` и `data/skills/`) |
| `skill_anthropic_standards.md` | Правила создания навыков по спецификации Anthropic Agent Skills: каталог `<name>/SKILL.md`, строгий frontmatter с типом навыка в `metadata.type`, progressive disclosure, вложенные файлы и скрипты; самодостаточный переносимый пакет - весь исполняемый код в `scripts/`, вывод скриптов по основному стандарту (plain, маркеры); запрет упоминаний навыков чужих репозиториев и кириллических плейсхолдеров; основной целевой инструмент - opencode; локальные навыки (`pr_loc`, префикс опционален) вне синхронизации - `data/skills/`, оба формата | Опционально - при создании, обновлении навыков формата SKILL.md (прежде всего для opencode; также Claude Code, Kilo Code) | Создание, обновление, миграция или редактирование навыков формата SKILL.md (подпапки `doc/skills/pg-<name>/` или `data/skills/`) |
| `opencode_tool_standards.md` | Правила создания кастомных tools opencode: расположение и именование, контракт `tool()` (схема аргументов, `description`), тонкая обертка с внешним скриптом, вывод и ошибки для агента, права и мутации; выбор между tool и skill | Опционально - при создании кастомных инструментов opencode | Создание, обновление или ревизия кастомных tools (`.opencode/tools/`, `~/.config/opencode/tools/`) |
| `software_doc_standards.md` | Шаблон документов установки ПО | Опционально - для документационных репозиториев | Написание документов установки приложений в репо документации |
| `index_nav_standards.md` | Канон навигационных документов агента: обязательный скелет индексов, правила качества карт маршрутизации, реестр индексов репозитория, структура AGENTS.md (слои, таксономия, каркас, политика CLAUDE.md) | Обязательно для всех репозиториев | Создание или правка любого индекса (`_index_*.md`, `sql/`, `diag/`, `test/`); создание или правка AGENTS.md; точки входа сессии |
| `repo_info_doc_standards.md` | Канон человеческих документов: README.md (структура, применимость секций, актуализация), правила инструкций и описаний для пользователя | Обязательно для всех репозиториев | Создание или правка README.md, инструкций и описаний для человека |

---

## Карта маршрутизации

Карта связывает тип задачи с документом пакета. Агент открывает указанные документы и применяет релевантные разделы.

| Тема задачи | Документ(ы) |
|-------------|-------------|
| Инициализация нового проекта, структура папок | `project_standards.md` (01, 07) |
| Бэкапы данных, папка `data/backup/` | `project_standards.md` (01.01) |
| Архивация вместо удаления: `.arch/` рядом с точкой накопления (сессии, задания, выгрузки), правила переноса | `project_standards.md` (01.01, 01.01.01) |
| Сессионные структуры, файл `SESSION.md`, идентификатор сессии, имя папки сессии, оверрайды и жизненный цикл сессии, архивация в `.arch/` | `project_standards.md` (01.01.01) |
| Проектные оверрайды стандарта, `doc/specs/overrides.md` | `project_standards.md` (01.01.02) |
| Индекс спецификаций `doc/specs/_index_specs.md` (обязателен), реестр файлов `doc/specs/` и сводка подпапок | `project_standards.md` (01.01); канон индексов - `index_nav_standards.md` (06.03) |
| Локальная витрина данных (SQLite): SQL-наборы `src_`/`report_`, режимы `local`/`refresh` | `project_standards.md` (01.01.03) |
| Диагностические скрипты `diag/` (подпапки по домену `diag/<domain>/`, маска `<domain>_<type>_<action>`, индекс `_index_diag.md`), разовые артефакты `tmp/` (подпапки `tmp/<domain>/`, скриншоты, выгрузки, уборка после коммита) | `project_standards.md` (01.01.04) |
| Эталонные тесты агентов `test/` (подпапки по домену `test/<domain>/`, индекс `_index_test.md`, согласование набора с заказчиком, граница с `diag/`), тестирование | `project_standards.md` (01.01.05, 06.04) |
| Конфигурация, config.ini, .env, пути | `project_standards.md` (01.02, 01.04) |
| Логирование, вывод, i18n, меню, GUI | `project_standards.md` (04); меню/rich UI - `console_ui_standards.md` |
| Dual-channel (`log_file_only`), rich-handler, интерактивный контекст (TTY / `CI` / `TERM=dumb`) | `project_standards.md` (04.04.02, 04.04.05, 04.06, 04.07); batch - `console_ui_standards.md` (04.04) |
| `--dry-run` для мутирующих CLI / `run/` | `project_standards.md` (03.04) |
| UI-эталон, макеты консоли и лога (`ui_spec.html`), кадр, клавиши, стабильность сквозных клавиш, роли, ввод при русской раскладке, сообщения сессии | `console_ui_standards.md` (02.01 API кадра, 02.03 клавиши, 04.03); якоря в `project_standards.md` (01.01, 04.04, 04.07, 07.03, 07.04) |
| Требования к тексту, оформление сообщений, стиль | `project_standards.md` (04.02, 04.03, 04.04); роли menu/rich - `console_ui_standards.md` |
| Документация и комментарии: каноничный нейминг, текст по факту, без оценок и историй, changelog только по запросу, пути без абсолютных (кроме футера ui_spec; в хабе-шаблоне футер - плейсхолдер `/home/<user>/<repo>/`) | `project_standards.md` (04.03); обновление доков - 07.01; футер ui_spec - `console_ui_standards.md` |
| Именование в коде (английский, без транслита; иерархия имени от общего к частному), слаги: латинский (kebab-case) и русский (кириллица, дефис) | `project_standards.md` (03.01, 04.03); навыки - `skill_plain_standards.md` (раздел 01.03) |
| Режим планирования, план-файлы | `project_standards.md` (04.03) |
| Зависимости: пиннинг, dev-зависимости (приоритет requirements.txt), локфайл uv.lock, ruff, mypy | `project_standards.md` (01.06) |
| Миграции схемы работающей БД (`schema_version`, `sql/migrations/`, db_migrate.py), запрет DROP + CREATE на живых данных | `project_standards.md` (03.05.03, 03.06.01) |
| Конвенции CLI: argparse, `--help` через t(), поведение на неизвестном флаге | `project_standards.md` (03.07) |
| Ротация лог-файлов, ключ `[logging] keep_last` | `project_standards.md` (04.06) |
| Отступление от логгера (вывод print): исчерпывающий список категорий - скрипты навыков (только полная эмуляция), временные разведочные скрипты, диагностические скрипты `run/` без изменения данных; два варианта формата по согласованию с пользователем - полная эмуляция (дата, время, маркер) или упрощенный вывод без метки времени и маркера уровня (типовой случай - деградация rich до plain); лог-файлы в отступлении не создаются | `project_standards.md` (04.06); канон скриптов навыков - `skill_anthropic_standards.md` (раздел 03.03) |
| Права на `.env` (0600) | `project_standards.md` (05.01) |
| Коды завершения (0/1/2/130/143), SIGTERM на Windows (console events) | `project_standards.md` (05.03) |
| Порядок принятия решений агентом, приоритет источников (стандарт / оверрайды / прецедент / интернет / практики), проверка фактов вместо выдумывания, ссылка на источник, незнакомый API, новая зависимость или версия, выбор подхода, необъяснимая ошибка, оверрайд каскада проверки | `project_standards.md` (07.06) |
| Термины пакета (сессия, витрина, кадр, роль, batch CLI и др.) | `project_standards.md` (08) |
| Безопасность, обработка ошибок, завершение | `project_standards.md` (05) |
| Git, сообщения коммитов | `project_standards.md` (06.01); формат версии пакета - 07.04; язык - русский (английский по запросу) |
| Явные ссылки AGENTS/CLAUDE на индексы репозитория (точки входа сессии) | `index_nav_standards.md` (02.02) |
| Структура AGENTS.md: принцип наполнения (маршрутизатор), слои static/repo-specific/generated-commands, таксономия разделов, каркас, политика CLAUDE.md | `index_nav_standards.md` (7) |
| README.md: структура, применимость секций, таблица структуры файлов, актуализация | `repo_info_doc_standards.md` |
| Создание или правка индекса любого каталога (`_index_*.md`, `sql/_index_sql.md`, `diag/_index_diag.md`, `test/_index_test.md`): скелет, реестр, карта маршрутизации, автоактуализация | `index_nav_standards.md` (03, 04, 05.01, 06) |
| Уточнение требований: пакетный и интерактивный режимы, выбор режима при начале диалога, вопросы интерактивного режима через стандартный агентский интерактив (в opencode - инструмент `question`), инкрементальная спецификация, декомпозиция спецификаций по модулям, жизненный цикл спек (стартовые до MVP, боевые, архивация стартовых в `.arch/`), триггер начала выполнения `ДЕЛАЙ` / `GO`, живая разведка данных на реальных базах и живой HTML-макет перед первой разработкой UI с данными; вопросы, спецификации | `project_standards.md` (07.02, 07.03) |
| Субагенты: делегирование точечных задач, роль основного агента | `project_standards.md` (07.01) |
| Редактирование стандарта, актуализация индекса | `project_standards.md` (07.05); канон индексов - `index_nav_standards.md` |
| Деплой как CLI-инструмент (общие правила) | `deploy_standards.md` |
| Деплой на Linux | `deploy_standards.md` + `deploy_lin_standards.md` (02.03 XDG_CONFIG_HOME в install.py) |
| Деплой на Windows | `deploy_standards.md` + `deploy_win_standards.md` |
| Миграция prod config.ini / .env при update.py - единый алгоритм | `deploy_standards.md` (04.01 - канон); платформенная специфика: `deploy_lin_standards.md` (03.05) / `deploy_win_standards.md` (05.05) |
| Batch CLI, двухстрочный rich-progress, status_col | `console_ui_standards.md` (03.02, 04.04); якорь 04.07.03 |
| Развертывание Docker-сервиса | `deploy_docker_standards.md` |
| Публикация портов Docker, привязка к интерфейсу (`127.0.0.1` / LAN-IP / интернет через reverse proxy), обход ufw демоном Docker, фильтрация через ufw-docker | `deploy_docker_standards.md` (02.06, 02.07, 03.01) |
| Адресное пространство Docker (`daemon.json`, `bip`, `default-address-pools`) | `deploy_docker_standards.md` (01.06) |
| Машинные оверрайды документации Docker (`override/<hostname>/`) | `deploy_docker_standards.md` (раздел 06) |
| Веб-адреса сервисов в summary профиле машины (`<hostname>_services_summary.md`) | `deploy_docker_standards.md` (06.03) |
| Веб-приложение: разработка, структура репозитория (`web/`), FastAPI, локальный запуск | `web_standards.md` (01, 02, 03, 04) |
| Несколько фронтов с одним бэкендом (наружный/локальный фронт): порты, привязка, префиксы, ассеты `fronts/<name>/` | `web_standards.md` (02.04, 07.04) |
| Деплой веб-приложения: модель хоста (systemd, nginx на хосте) или модель Docker (compose) | `web_standards.md` (05, 06) |
| Конфигурация nginx веб-приложения: server block, проксирование, статика | `web_standards.md` (07) |
| TLS веб-приложения: certbot/ACME (VPS), self-signed (LAN) | `web_standards.md` (08) |
| Обновление и откат веб-приложения, health-check | `web_standards.md` (10) |
| Создание или правка plain-навыка агента (плоский формат, `doc/skills/` и `data/skills/`) | `skill_plain_standards.md` |
| Типы навыков (таксономия): хаб-навык `hub_`/`hub-` (слаг `hub_loc`), общий `glob_`/`glob-` (слаг `hub_glob`), проектный `pg_`/`pg-` (слаг `pr_glob`), локальный `pl_`/`pl-` или без префикса (слаг `pr_loc`, оба формата, `data/skills/`); фиксация типа в заголовке навыка (`type` / `metadata.type`) при создании, обновлении и миграции | `skill_plain_standards.md` (разделы 01.03, 02.01.01); `skill_anthropic_standards.md` (раздел 02.01) |
| Навык формата Anthropic Agent Skills (каталог SKILL.md, frontmatter, bundled scripts, весь код в `scripts/`, самодостаточный пакет; целевой инструмент - opencode) | `skill_anthropic_standards.md` |
| Пара форматов навыка (SKILL.md + исходник в `templates/` каталога), канон пары, перенос кода и внешних скриптов при миграции | `skill_anthropic_standards.md` (разделы 01.03, 03.03) |
| Локальный навык (`pr_loc`, `data/skills/`, оба формата - plain-файл и каталог SKILL.md, префикс опционален, коммитится, индекс - `data/skills/_index_skills_pl.md`) | `skill_plain_standards.md` (разделы 01.02, 01.03), `skill_anthropic_standards.md` (раздел 01.03) |
| Индексация навыков (сводный, порепозиторный, индекс локальных навыков `data/skills/_index_skills_pl.md`; кто ведет) | `index_nav_standards.md` (раздел 06.02) |
| Создание или правка кастомного tool opencode (`.opencode/tools/`, `~/.config/opencode/tools/`): контракт `tool()`, схема аргументов, `description`, вывод и ошибки, права | `opencode_tool_standards.md` |
| Выбор между кастомным tool и навыком для операции агента | `opencode_tool_standards.md` (раздел 01.02); навыки - `skill_anthropic_standards.md` |
| Документ установки приложения в репо документации | `software_doc_standards.md` |

---

## Политика версионирования

Пакет стандартов имеет единую версию - семантическое версионирование (SemVer) `MAJOR.MINOR.PATCH`. Версия едина для всего пакета: индивидуальные версии документов упразднены.

Формат заголовка каждого документа: `# <document_name>. Версия X.Y.Z`. Дата в заголовках не используется; фактическая дата изменения документа доступна в git-истории.

**Правила изменения версии:**

- **PATCH** (X.Y.Z -> X.Y.Z+1) - уточнения формулировок, исправление опечаток, форматирование, правки примеров, добавление элементов в существующие списки
- **MINOR** (X.Y.Z -> X.Y+1.0) - новые разделы, новые правила, новые требования в рамках существующих документов, структурные изменения внутри документа
- **MAJOR** (X.Y.Z -> X+1.0.0) - добавление или удаление документов пакета, несовместимые изменения структуры, несовместимые изменения требований, изменение назначения или области действия документа, смена модели (например, переход на единую версию пакета)

**Bump версии пакета - только по явному запросу пользователя** («подними версию», «bump» и аналоги). Агент при правке содержимого `doc/standards/` **не** поднимает версию сам и не требует bump как обязательный шаг завершения задачи.

При запросе bump:

1. Поднять версию пакета по правилам PATCH/MINOR/MAJOR выше (если пользователь не указал уровень)
2. Поставить новую версию во всех локациях (одно значение):
   - `doc/standards/_index_standards.md` (этот файл)
   - все документы `doc/standards/*.md` (заголовки)
   - `AGENTS.md` (строка `Соответствует стандарту:`)
   - `README.md` (строка `Соответствует стандарту:`)
   - `CLAUDE.md` (строка `Соответствует стандарту:`) - только если файл есть в репозитории

Реестром версии служит этот индекс. Когда версии проставлены, во всех **присутствующих** перечисленных локациях они должны совпадать. Правка содержимого стандартов без bump номера допустима; актуализация реестра и карты маршрутизации в `_index_standards.md` при изменении файлов пакета обязательна (см. pre-commit и раздел «Автоактуализация индекса»).

---

## Автоактуализация индекса

Если пользователь тегнул этот файл (`@doc/standards/_index_standards.md`) без конкретной команды или задачи, агент выполняет автоактуализацию:

1. Прочитать все документы в `doc/standards/*.md` (кроме этого индекса)
2. Извлечь из каждого документа: имя, назначение, применимость, версию из заголовка
3. Обновить реестр документов: актуальные описания, применимость, триггеры
4. Обновить карту маршрутизации: актуальные темы задач и ссылки на разделы
5. Проверить единую версию пакета во всех присутствующих локациях: документы `doc/standards/*.md`, `AGENTS.md`, `README.md`, `CLAUDE.md` (если есть) - должны иметь одинаковую версию
6. Вывести отчет: какие документы обновлены, статус версии пакета, расхождения версий если есть
